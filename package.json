{
  "name": "url-shortener",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client-app/url-shortner-app && npm install && npm run build && cd ../../server-app && npm install && npm run build",
    "start": "cd server-app && npm start",
    "dev:client": "cd client-app/url-shortner-app && npm run dev",
    "dev:server": "cd server-app && npm run dev"
  },
  "dependencies": {
    "axios": "^1.6.7",
    "react-router-dom": "^6.22.1"
  }
}
